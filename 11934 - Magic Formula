#include<iostream>
#include<cstdio>
using namespace std;
int main()
{
    int a,b,c,d,l,i,cnt;
    while(cin>>a>>b>>c>>d>>l && !(a==0 &&b==0 && c==0 && d==0 && l==0))
    {
        cnt=0;
        for(i=0; i<=l ;i++)
        {
            if((a*i*i+b*i+c)%d==0)
                cnt++;
        }
        cout<<cnt<<endl;


    }
    return 0;
}
